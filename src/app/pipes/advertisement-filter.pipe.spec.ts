import { AdvertisementFilterPipe } from './advertisement-filter.pipe';

describe('AdvertisementFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new AdvertisementFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
